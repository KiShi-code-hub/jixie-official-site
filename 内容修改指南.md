# 计协官网内容修改指南

这份文档教协会同学怎么修改网站内容，不需要懂编程也能照着做。

## 快速结论

网站内容分三类，改的地方也不同：

| 想改什么 | 去哪里改 |
| --- | --- |
| 招新公告 | `src/content/announcements/` |
| 活动动态 | `src/content/activities/` |
| 技术项目 | `src/content/projects/` |
| 网站 Logo | `public/logo.png` |
| 首页/活动照片 | `public/images/` |
| 联系方式 | `src/pages/contact.astro` 和 `src/components/Footer.astro` |
| 首页文字 | `src/pages/index.astro` |

## 什么是 Markdown

`.md` 文件是纯文本，用简单符号排版：

```markdown
# 一级标题
## 二级标题

这是普通段落。

- 列表项 1
- 列表项 2
```

每个内容文件开头有一段被 `---` 包起来的信息，叫“元信息”，用来记录标题、日期、分类等：

```markdown
---
title: "2026 年秋季招新公告"
date: "2026-09-05"
category: "招新"
summary: "一句话简介，会显示在首页和列表上"
---

这里是正文内容。
```

## 一、修改招新公告

1. 打开 `src/content/announcements/2026-秋季招新公告.md`。
2. 修改 `---` 里的标题、日期、摘要。
3. 修改 `---` 下面的正文。
4. 保存文件。
5. 在项目目录运行 `npm run dev` 预览。

如果以后发布新的招新公告，直接新建一个 `.md` 文件放进 `announcements/`，格式照抄现有文件即可，网站会自动读取。

## 二、新增一条活动

1. 复制 `src/content/activities/` 里任意一个 `.md` 文件。
2. 重命名为新的活动名，例如 `2026-冬季经验分享会.md`。
3. 修改文件里的：
   - `title`：活动标题
   - `date`：活动日期
   - `location`：活动地点
   - `category`：分类，例如 `技术`、`讲座`、`招新`
   - `summary`：活动简介
4. 在正文写活动详情。
5. 保存后首页“近期活动”和“活动动态”页面会自动出现这一条。

## 三、新增一个项目

方法同上：

1. 复制 `src/content/projects/` 里任意一个 `.md` 文件。
2. 改 `title`、`category`、`status`、`tech`、`summary`。
3. `tech` 是技术标签列表，按这种格式写：

```markdown
tech: ["Astro", "Markdown", "Docker"]
```

## 四、换 Logo 和照片

保持文件名不变，直接覆盖文件：

| 位置 | 文件 |
| --- | --- |
| 网站 Logo | `public/logo.png` |
| 首页大图 | `public/images/hero.png` |
| 关于页图片 | `public/images/about.png` |
| 活动卡片图 | `public/images/activity-1.png` |
| 活动卡片图 | `public/images/activity-2.png` |
| 活动卡片图 | `public/images/activity-3.png` |

建议照片先压缩成宽度 1200 到 1600 像素的 JPG/PNG，否则首页加载会慢。

## 五、修改联系方式

联系方式和页脚在 2 个地方，两处都要改：

- `src/pages/contact.astro`：联系页
- `src/components/Footer.astro`：每个页面底部的页脚

打开文件后找到 `123456789`、`jixie@example.com`、`学院信息楼 2 层` 这些示例内容，替换成真实信息即可。

## 六、修改首页文字

打开 `src/pages/index.astro`，直接修改引号里的中文即可，例如：

```astro
<h1>计算机协会<br />把热爱写进代码</h1>
```

改成你们的协会口号后保存。

## 七、预览修改效果

在项目目录打开 PowerShell，运行：

```powershell
npm install
npm run dev
```

然后浏览器访问 `http://localhost:4321`。修改内容后页面会自动刷新。

## 八、把修改发布出去

改完后需要提交到 Git 并让服务器更新：

```powershell
git add .
git commit -m "更新招新公告"
git push
```

负责服务器的人执行：

```bash
cd 计协官网设计
git pull
sudo docker compose -f deploy/docker-compose.yml up -d --build
```

如果只是改了 `public/` 里的图片，也可以直接替换服务器上网站目录里的图片文件，但用 Git 更新更规范、有历史记录。

## 九、不要动的文件

- `node_modules/`：依赖包，`npm install` 自动生成
- `dist/`：构建结果，`npm run build` 自动生成
- `.astro/`：自动生成的类型文件
- `package-lock.json`：依赖锁定文件

## 十、常见问题

**问：为什么我改了 Markdown，网站没有变化？**

答：确认保存的是 `src/content/` 下的文件，且 `npm run dev` 正在运行；如果部署在服务器上，还需要执行 `git push` 和服务器构建。

**问：新增内容文件为什么显示空白？**

答：检查文件开头是否有完整的 `---` 元信息，字段名和已有文件保持一致，日期用 `"2026-09-05"` 这种格式。

**问：图片可以叫别的名字吗？**

答：可以，但要同时修改页面代码里的路径，建议先沿用现在的文件名，最省事。

**问：我想改主题颜色怎么办？**

答：打开 `src/styles/global.css`，修改最上面的颜色变量，例如 `--accent: #d64b2a;`。
