# 计算机协会官网建站全流程方案

## 一、需求理解（默认假设）

按你的描述，我先按以下理解来规划：

- 网站定位：对外展示协会形象、活动动态、部门介绍、技术分享、招新信息。
- 访问对象：校内外师生、潜在新成员、合作方。
- 维护者：协会技术部成员，可能有非程序员参与内容维护。
- 现有条件：域名在 Cloudflare（DNS 或注册均在 Cloudflare），有一台服务器但没有公网 IP。
- 当前目录：`计协官网设计`，目前为空，可作为官网项目开发目录。

这些假设如果与你实际情况不符，后面的方案中我预留了决策点。

## 一.五、已确认的实际配置

截至 2026-08-19，你已确认的信息：

- 主域名：`kishi.uk`，打算新建一个子域名放官网（子域名名称待定，建议用 `club.kishi.uk` 或 `it.kishi.uk`）。
- 官网子域名：`mzjx.kishi.uk`。
- 服务器：Ubuntu Server 26（LTS），尚未安装 Docker，可以后续安装。
- 首页必备内容：协会 Logo、协会照片、招新公告。
- 网站用途：对外展示协会形象，方便校内同学了解协会、参与招新。

内容更新方式：静态站 + Git（已确认）。

## 二、核心结论

1. **用 Cloudflare Tunnel 解决无公网 IP 的问题。** 服务器主动向 Cloudflare 边缘节点建立出站隧道，域名通过 Cloudflare DNS 解析，访问者访问 Cloudflare 边缘，再由隧道转发到你内网服务器。不需要公网 IP，不需要开放入站端口，还自带 HTTPS。
2. **建站形态建议从“静态/轻量站点”起步。** 协会官网以展示为主，静态站点部署简单、安全、速度快、好维护；如果确定要频繁后台发文，再换成 WordPress；如果要做报名、互动等功能，选 Next.js 全栈。
3. **把内容更新流程固化下来。** 无论哪种方案，都要写清“谁改内容、改哪里、怎么发布”，否则毕业换届后网站就没人维护了。

## 三、技术路线选择

| 方案 | 适合情况 | 优点 | 代价 |
| --- | --- | --- | --- |
| A. 静态站（Astro / Hugo） | 展示型官网，更新量不大 | 快、安全、部署简单、免费托管可行 | 发文章需要懂一点 Git / Markdown |
| B. WordPress + cloudflared | 非程序员经常后台发文 | 后台完善，编辑器友好 | 性能、插件安全、服务器维护成本更高 |
| C. Next.js 全栈 | 需要报名、登录、动态数据 | 功能最灵活，适合技术部练手 | 复杂度、长期维护成本最高 |

**我的推荐：先做方案 A，静态站 + Git 管理；如果协会里有同学愿意维护服务器，后续再局部加 CMS 或动态功能。** 如果你认为协会干部以后只会用网页后台发文章，那就直接选 B。

### “静态站 + Git”和“WordPress 后台”有什么区别

简单来说，区别是“改网页的方式”和“谁负责维护服务器”。

**静态站 + Git（方案 A）**

- 网站内容以 Markdown / 网页源码文件存在 Git 仓库里。
- 每次更新 = 有人编辑内容文件，推送到 Git 仓库，然后构建并发布到服务器。
- 优点：网站没有数据库和动态后台，速度快、不容易被攻击、服务器负担小，成本低。
- 缺点：更新需要会一点 Git / Markdown；如果想让完全不懂代码的同学发文章，需要额外加一个后台编辑界面（例如 Decap CMS）或做一套简单后台。

**WordPress 后台（方案 B）**

- 网站自带一个网页后台，类似写公众号文章：登录后台、填标题、插图、点发布，不需要懂代码。
- 优点：内容编辑门槛最低，招新公告、活动新闻谁都可以发。
- 缺点：服务器要常驻 PHP + MySQL，需要定期更新 WordPress、插件和主题；被攻击面更大，维护工作量更高。

对你们计协来说，技术部成员通常能接受 Git / Markdown，所以方案 A 更省心。如果以后想让宣传干事直接用网页发公告，再给方案 A 加一个轻量后台，或切换成方案 B 都不迟。

## 四、网站结构建议

```text
首页
├── 关于协会（简介、宗旨、历史）
├── 部门与成员（部门介绍、骨干名单）
├── 活动动态（活动回顾、新闻）
├── 技术分享（教程、项目、作品展示）
├── 招新（加入方式、报名入口）
└── 联系我们（QQ群、邮箱、公众号、地址）
```

首页建议放：协会名称与 slogan、近期活动、部门入口、招新提示。首屏就要让别人看懂“我们是谁”。

## 五、全流程实施步骤

### 阶段一：准备

1. 确认 Cloudflare 账号已接管域名：域名的 Nameserver 是 Cloudflare 的，或者域名直接注册在 Cloudflare。
2. 确认服务器操作系统：你已确认使用 Ubuntu Server 26 LTS；服务器能正常出网访问 `cloudflare.com` 即可。
3. 服务器上安装 Docker 和 Git（如果选方案 C，还需要 Node.js 环境）。
4. 建一个 Git 仓库（GitHub / Gitee 均可），用于保存官网代码和历史版本。

### 阶段二：开发建站

5. 初始化 Astro（或 Hugo / Next.js），建立页面结构和统一样式。
6. 设计视觉：确定协会主色调、Logo 位置、导航、卡片布局；使用清晰真实的协会照片，不要用纯占位素材。
7. 填充内容：协会介绍、部门职责、活动照片、联系方式、招新文案。
8. 本地运行 `npm run dev`（或对应命令）预览页面。

### 阶段三：部署到内网服务器

9. 在服务器上构建站点，例如 `docker build` 或 `npm run build`。
10. 用 Nginx 或 Caddy 监听 `127.0.0.1:8080`（或容器映射端口），托管静态文件 / 反向代理应用。
11. 服务器本机验证：`curl http://127.0.0.1:8080` 能正常返回页面。

### 阶段四：Cloudflare Tunnel 接入

12. 在服务器上安装 `cloudflared`。
13. 执行 `cloudflared tunnel login`，按提示授权你要用的域名。
14. 创建隧道：`cloudflared tunnel create jixie-official`，记录隧道 ID。
15. 编写 `config.yml`，示例：

```yaml
tunnel: <你的隧道ID>
credentials-file: /root/.cloudflared/<隧道ID>.json

ingress:
  - hostname: www.example.com
    service: http://127.0.0.1:8080
  - service: http_status:404
```

16. 绑定子域名：假设你选用 `club.kishi.uk`，执行 `cloudflared tunnel route dns <隧道ID> club.kishi.uk`，Cloudflare 会自动创建 CNAME 记录。
17. 设为系统服务开机自启：`cloudflared service install`。
18. 浏览器访问 `https://club.kishi.uk` 验证网站是否正常；如果需要，再把根域名 `kishi.uk` 重定向到该子域名。

### 阶段五：安全与性能

19. 在 Cloudflare 中把 SSL/TLS 加密模式设为 **Full (strict)**，并让源站也启用 HTTPS（可用 Caddy 自动申请证书，或用自签证书）。
20. 开启免费 WAF 规则、缓存规则，静态资源（图片、CSS、JS）设置长缓存。
21. 管理后台、SSH 使用强密码或密钥登录；任何管理页面建议用 Cloudflare Access 加一层登录保护。
22. 服务器防火墙只放行必要的管理端口；隧道方案不需要对外开放 80/443 入站端口。
23. 定期系统更新，备份站点源码、上传文件、数据库、`cloudflared` 配置和凭证。

### 阶段六：内容与运营

24. 固定内容更新流程：例如“成员在仓库里改 Markdown -> 推送到 main -> 自动构建部署”；如果选 WordPress，则写清后台发文规范。
25. 添加 `sitemap.xml`、`robots.txt`，开启 Cloudflare Analytics 做基础统计。
26. 向搜索引擎提交收录（百度、必应等）。
27. 建立协会内部维护文档：服务器地址、登录方式、域名管理、发布流程、换届交接清单。

## 六、关键注意事项

- **无公网 IP 不是障碍。** Cloudflare Tunnel 由服务器主动向外连接 Cloudflare，只需服务器能出网（443 端口），不需要公网 IP，也不需要端口映射。
- **国内访问速度可能不稳定。** Cloudflare 免费版在国内没有原生边缘节点，校外访问可能较慢。建议校内访问用内网地址直连，校外走隧道；如果以后要把网站迁到国内云服务器，就需要 ICP 备案。
- **备案问题。** 域名实名是必须的；如果网站部署在国内服务器或使用国内 CDN，必须完成 ICP 备案。使用 Cloudflare Tunnel 且访问主要面向校园网/海外节点时，通常不强制备案，但建议向学校网络中心或咨询清楚再上线。
- **换届维护是最大风险。** 域名、服务器、Cloudflare 账号、Git 仓库的权限都要做交接清单，避免前任负责人毕业后面临失联。
- **备份永远要有。** 至少备份：源码仓库、数据库（如有）、上传图片、隧道配置、Cloudflare 域名设置说明。

## 七、下一步需要你确认的问题

1. 子域名名称：`club.kishi.uk`、`it.kishi.uk`，还是其他名字？
2. 内容更新方式：选“静态站 + Git”（推荐）还是“WordPress 后台发文”？
3. 首页素材：协会 Logo 文件、协会照片、招新时间与文案，是否已准备好？

确认后，我可以在当前目录直接搭建官网原型、写部署脚本，并给出一份服务器上的 cloudflared 隧道配置模板。

## 八、项目进展（2026-08-19）

- 已确认官网子域名：`mzjx.kishi.uk`。
- 已确认内容更新方式：静态站 + Git。
- 已在 `计协官网设计` 目录完成 Astro 静态站原型，包含首页、关于、活动、项目、招新、联系 6 个页面。
- 动态内容（招新公告、活动、项目）已接入 Markdown 内容集合，后续改内容直接编辑 `src/content/` 下文件。
- 已提供 Docker 部署文件（`deploy/Dockerfile`、`deploy/docker-compose.yml`、`deploy/nginx.conf`）和 Cloudflare Tunnel 配置模板（`deploy/cloudflared-config.yml`）。
- 素材进度：`public/logo.png` 已替换为协会真实 Logo（自动压缩为网页版）；`public/images/` 下占位图仍待替换为真实照片。
