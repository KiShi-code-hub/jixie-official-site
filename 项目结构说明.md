# 计协官网项目结构说明

这份文档把 `计协官网设计` 文件夹从上到下逐级讲清楚，方便协会同学快速知道每个文件夹是干什么的。

## 先记住一句话

平时维护网站，绝大多数情况只动 3 个地方：

1. `src/content/`：改招新公告、活动、项目
2. `public/`：换 Logo 和照片
3. `src/pages/`：改首页、关于、联系等页面的文字（不常改）

其余文件夹基本都是“装网站引擎和配置”的，不用主动去碰。

## 完整目录树

```text
C:\Users\jqx\Desktop\计协官网设计
│
├─ .git/                        Git 版本库，自动生成，不要手动改
├─ .gitignore                   Git 忽略规则，不需要手动改
├─ README.md                    项目快速说明
├─ 建站全流程方案.md             最初的整体建站方案
├─ 项目结构说明.md               本文档
├─ 内容修改指南.md               改内容教学
│
├─ package.json                 项目信息和命令清单
├─ package-lock.json            依赖版本锁定文件，不要手动改
├─ astro.config.mjs             网站配置，目前配置了 mzjx.kishi.uk
├─ tsconfig.json                TypeScript 配置，一般不用动
│
├─ public/                      ★ 会原样发布到网站上的静态文件
│  ├─ logo.png                  协会 Logo（已替换成你的 logo.png 压缩版）
│  ├─ robots.txt                搜索引擎爬取规则
│  └─ images/                   网站图片
│     ├─ hero.png               首页大图（待换真实照片）
│     ├─ about.png              关于页图片（待换真实照片）
│     ├─ activity-1.png         活动卡片图 1（待换）
│     ├─ activity-2.png         活动卡片图 2（待换）
│     └─ activity-3.png         活动卡片图 3（待换）
│
├─ src/                         ★ 网站源码
│  ├─ content.config.ts         内容集合配置，一般不用动
│  ├─ content/                  ★ 平时改内容最常进的目录
│  │  ├─ announcements/         招新公告，里面是一个个 .md 文件
│  │  ├─ activities/            活动动态，里面是一个个 .md 文件
│  │  └─ projects/              技术项目，里面是一个个 .md 文件
│  ├─ components/               页面公共组件
│  │  ├─ Header.astro           顶部导航栏
│  │  ├─ Footer.astro           页脚
│  │  ├─ Icon.astro             小图标组件
│  │  ├─ SectionHeading.astro   小节标题
│  │  ├─ ActivityCard.astro     活动卡片
│  │  └─ ProjectCard.astro      项目卡片
│  ├─ layouts/
│  │  └─ BaseLayout.astro       全站页面骨架（标题、导航、页脚）
│  ├─ pages/                    ★ 每个页面一个文件
│  │  ├─ index.astro            首页
│  │  ├─ about.astro            关于我们
│  │  ├─ activities.astro       活动动态
│  │  ├─ projects.astro         技术项目
│  │  ├─ join.astro             招新
│  │  └─ contact.astro          联系我们
│  └─ styles/
│     └─ global.css             全站样式，控制颜色、字体、布局
│
├─ scripts/
│  └─ generate-placeholders.mjs 重新生成占位图的小工具
│
└─ deploy/                      服务器部署文件
   ├─ Dockerfile                网站镜像构建规则
   ├─ nginx.conf                Nginx 网站配置
   ├─ docker-compose.yml        一键启动网站容器
   └─ cloudflared-config.yml    Cloudflare 隧道配置模板
```

## 各文件夹详细说明

### `public/`：直接放进网站根目录的文件

`public` 里的内容会原样出现在网站上。也就是说：

- 网站根路径下的 Logo 就是 `public/logo.png`
- 首页大图就是 `public/images/hero.png`

所以换图片、换 Logo，直接替换这里的同名文件即可，页面代码不需要改。

### `src/content/`：网站内容的“素材库”

这是协会同学最常用的目录，里面全是 `.md`（Markdown）文本文件：

- `announcements/` 放招新公告
- `activities/` 放活动
- `projects/` 放项目

每个 `.md` 文件就是一篇文章。新增一个文件，网站就会自动多一条内容。

### `src/pages/`：每个页面长什么样

`pages` 里每个 `.astro` 文件对应网站上的一个页面：

- `index.astro` 对应首页 `/`
- `about.astro` 对应 `/about/`
- 其他以此类推

一般不建议不懂代码的同学直接改这里，但简单的文字替换是可以的，改完保存即可。

### `src/components/`：网站公共部件

导航栏、页脚、图标、活动卡片、项目卡片都放在这里。这些部件会被多个页面共用，所以只在这里改一次，所有页面都会跟着变。

### `src/layouts/`：页面公共骨架

`BaseLayout.astro` 决定了每个页面的标题、编码、导航栏、页脚等公共部分，一般不用改。

### `src/styles/global.css`：全站样式

网站的颜色、字体、按钮、卡片、间距都在这一个文件里控制。想整体换主题色或调整布局时改这里。

### `deploy/`：服务器部署相关

这些文件是给服务器用的，本地预览不需要它们：

- `Dockerfile`：把网站打包成镜像
- `docker-compose.yml`：在服务器上一条命令启动网站
- `nginx.conf`：网站如何被访问
- `cloudflared-config.yml`：无公网 IP 时连接 Cloudflare 的隧道配置

## 自动生成、不要手动改的文件夹

```text
node_modules/   运行项目需要的依赖包，由 npm install 自动生成
dist/           构建结果，由 npm run build 自动生成，发布的就是它
.astro/         Astro 自动生成的类型文件
.git/           Git 版本历史
```

这些文件夹里面的东西都不是手写的源码，删了或改坏了会让项目无法运行。

## 文件后缀是什么意思

- `.astro`：Astro 页面/组件文件，可以混写 HTML 和部分 JavaScript
- `.md`：Markdown 纯文本，最适合写文章
- `.css`：样式文件
- `.mjs`：JavaScript 脚本
- `.json`：配置文件
- `.yml`：服务器部署配置

